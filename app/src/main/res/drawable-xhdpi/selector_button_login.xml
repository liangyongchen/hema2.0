<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 按下去显示的图片 -->
    <item android:drawable="@drawable/login_btn_s2" android:state_pressed="true"></item>
    <!-- 没按下去显示的图片 -->
    <item android:drawable="@drawable/login_btn_s3" android:state_pressed="false"></item>
    <!-- 默认情况下显示的图片 -->
    <item android:drawable="@drawable/login_btn_s3"></item>

</selector>